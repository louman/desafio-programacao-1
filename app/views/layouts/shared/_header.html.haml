%header
  %nav.top-bar{"data-topbar" => "", :role => "navigation"}
    %ul.title-area
      %li.name
        %h1
          %a{:href => "#"} Myfreecomm
      %li.toggle-topbar.menu-icon
        %a{:href => "#"}
          %span Menu
    %section.top-bar-section
      %ul.right
        
        - if user_signed_in?
          %li
            %a{:href => uploads_path} Upload
            %li.divider
          %li.has-dropdown
            %a{:href => "#"} Olá, #{current_user.name}
            %ul.dropdown
              %li= link_to('Sair', destroy_user_session_path, method: :delete)
        - else
          %li.has-form
            = link_to "Entrar", user_omniauth_authorize_path(:google_oauth2), class: 'button'